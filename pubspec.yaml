name: my_firebase_udval_teacher
description: "A new Flutter project."

publish_to: "none" # Remove this line if you wish to
version: 1.0.0+1

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  firebase_core:
  firebase_database:
  g_json:

  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
